package com.vaultia.app.core.model

/**
 * Domain model boundary for Vaultia.
 *
 * Future responsibilities:
 * - vault item models;
 * - metadata models;
 * - validation models.
 *
 * Sensitive fields must be explicitly classified before implementation.
 */
internal object ModelPackageInfo
