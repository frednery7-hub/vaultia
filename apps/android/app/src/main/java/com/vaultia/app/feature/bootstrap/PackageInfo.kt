package com.vaultia.app.feature.bootstrap

/**
 * Bootstrap feature boundary.
 *
 * Future responsibilities:
 * - first app launch flow;
 * - local-only warning;
 * - no recovery acknowledgement;
 * - initial vault setup entry point.
 *
 * This package must not create or persist vault secrets yet.
 */
internal object BootstrapPackageInfo
