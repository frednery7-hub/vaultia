# AndroidX support — required by modern Android dependencies (including
# transitive dependencies pulled in by com.lambdapioneer.argon2kt:argon2kt,
# evaluated in Phase 22). Project did not previously declare this file
# because no dependency before Phase 22 required it.
android.useAndroidX=true
